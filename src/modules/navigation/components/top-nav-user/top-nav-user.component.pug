li.nav-item.dropdown.no-caret.mr-3.sb-dropdown-user(
    *ngIf='userService.user$ | async as user',
    ngbDropdown,
    [placement]='placement',
    display='dynamic'
)
    a#navbarDropdownUserImage.btn.sb-btn-icon.sb-btn-transparent-dark.dropdown-toggle(
        ngbDropdownToggle,
        data-cy='userMenu',
        role='button',
        aria-haspopup='true',
        aria-expanded='false'
    )
        img.img-fluid([src]='user.image')

    .dropdown-menu.border-0.shadow.animated--fade-in-up(
        ngbDropdownMenu,
        aria-labelledby='dropdownUser'
    )
        h6.dropdown-header.d-flex.align-items-center
            img.sb-dropdown-user-img([src]='user.image')
            .sb-dropdown-user-details
                .sb-dropdown-user-details-name {{ user.firstName }} {{ user.lastName }}
                .sb-dropdown-user-details-email {{ user.email }}
        .dropdown-divider
        a.dropdown-item(routerLink='/dashboard')
            .sb-dropdown-item-icon
                i-feather(name='settings')
            | Account
        a.dropdown-item(routerLink='/dashboard')
            .sb-dropdown-item-icon
                i-feather(name='log-out')
            | Logout
