ng-container(*ngIf='!rtl')
    span([ngClass]='{"sb-nav-fixed": !static}')
        sb-top-nav
        #layoutSidenav
            #layoutSidenav_nav
                sb-side-nav(
                    [sidenavStyle]='sidenavStyle',
                    [sideNavItems]='sideNavItems',
                    [sideNavSections]='sideNavSections'
                )
            #layoutSidenav_content(
                [ngClass]='{"scrollable": !static}',
                (click)='closeSideNavIfOpen()'
            )
                main    
                    ng-container(*ngTemplateOutlet='defaultContent')
                sb-footer

ng-container(*ngIf='rtl')
    span.sb-layout-rtl([ngClass]='{"sb-nav-fixed": !static}')
        sb-top-nav([rtl]='true')
        #layoutSidenav
            #layoutSidenav_content(
                [ngClass]='{"scrollable": !static}',
                (click)='closeSideNavIfOpen()'
            )
                main    
                    ng-container(*ngTemplateOutlet='defaultContent')
                sb-footer
            #layoutSidenav_nav
                sb-side-nav(
                    [sidenavStyle]='sidenavStyle',
                    [sideNavItems]='sideNavItems',
                    [sideNavSections]='sideNavSections'
                )


ng-template(#defaultContent)
    ng-content